<Page
    x:Class="EwCalc.Views.JammingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:helpers="using:EwCalc.Helpers"
    xmlns:local="using:EwCalc.Views">

    <Page.Resources>
        <helpers:FieldErrorToBrushConverter x:Key="FieldErrorToBrush" />
        <helpers:FieldErrorToTooltipConverter x:Key="FieldErrorToTooltip" />
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24,20,24,24" Spacing="8">

            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Jamming" Style="{ThemeResource TitleTextBlockStyle}" />
                    <TextBlock Text="J/S ratio · Burnthrough · Partial-band optimum bandwidth"
                               Style="{ThemeResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </StackPanel>
                <Button Grid.Column="1" VerticalAlignment="Top"
                        Click="CopyResultsButton_Click"
                        AutomationProperties.Name="Copy results to clipboard"
                        ToolTipService.ToolTip="Copy all results to clipboard">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE8C8;" FontSize="14" />
                        <TextBlock Text="Copy results" />
                    </StackPanel>
                </Button>
            </Grid>

            <StackPanel Spacing="8">
                <TextBlock Text="SIGNAL" Style="{StaticResource SectionHeaderStyle}" />
                <Border Style="{StaticResource InputCardStyle}">
                    <StackPanel Spacing="12">
                        <NumberBox x:Name="SignalErpBox"    Header="Signal ERP (dBm)"
                                   AutomationProperties.Name="Signal ERP (dBm)"
                                   ToolTipService.ToolTip="Effective radiated power of the desired signal: transmitter power + antenna gain"
                                   Value="{x:Bind ViewModel.DefaultSignalErp, Mode=OneTime}"
                                   Minimum="-100" Maximum="200" SmallChange="1"
                                   BorderBrush="{x:Bind ViewModel.SignalErpError, Mode=OneWay,
                                       Converter={StaticResource FieldErrorToBrush}}"
                                   ValueChanged="SignalErpBox_ValueChanged" />
                        <NumberBox x:Name="SignalDistBox"   Header="Signal→Rx distance (km)"
                                   AutomationProperties.Name="Signal to Rx distance (km)"
                                   ToolTipService.ToolTip="Ground range from the signal transmitter to the receiver"
                                   Value="{x:Bind ViewModel.DefaultSignalToRxDist, Mode=OneTime}"
                                   Minimum="0.01" Maximum="10000" SmallChange="1"
                                   BorderBrush="{x:Bind ViewModel.SignalDistError, Mode=OneWay,
                                       Converter={StaticResource FieldErrorToBrush}}"
                                   ValueChanged="SignalDistBox_ValueChanged" />
                        <NumberBox x:Name="SignalHeightBox" Header="Signal Tx height (m)"
                                   AutomationProperties.Name="Signal Tx height (m)"
                                   ToolTipService.ToolTip="Signal transmitter antenna height above ground — determines the propagation model (LOS or 2-ray) and Fresnel zone crossover distance"
                                   Value="{x:Bind ViewModel.DefaultSignalTxHeight, Mode=OneTime}"
                                   Minimum="0.1" Maximum="100000" SmallChange="1"
                                   BorderBrush="{x:Bind ViewModel.SignalHeightError, Mode=OneWay,
                                       Converter={StaticResource FieldErrorToBrush}}"
                                   ValueChanged="SignalHeightBox_ValueChanged" />
                        <NumberBox x:Name="SignalBwBox"     Header="Signal bandwidth (MHz)"
                                   AutomationProperties.Name="Signal bandwidth (MHz)"
                                   ToolTipService.ToolTip="Occupied bandwidth of the target signal — used for partial-band jamming optimisation"
                                   Value="{x:Bind ViewModel.DefaultSignalBandwidth, Mode=OneTime}"
                                   Minimum="0.001" Maximum="1000" SmallChange="0.01"
                                   BorderBrush="{x:Bind ViewModel.SignalBandwidthError, Mode=OneWay,
                                       Converter={StaticResource FieldErrorToBrush}}"
                                   ValueChanged="SignalBwBox_ValueChanged" />
                    </StackPanel>
                </Border>

                <TextBlock Text="JAMMER" Style="{StaticResource SectionHeaderStyle}" />
                <Border Style="{StaticResource InputCardStyle}">
                    <StackPanel Spacing="12">
                        <NumberBox x:Name="JammerErpBox"    Header="Jammer ERP (dBm)"
                                   AutomationProperties.Name="Jammer ERP (dBm)"
                                   ToolTipService.ToolTip="Effective radiated power of the jammer toward the receiver"
                                   Value="{x:Bind ViewModel.DefaultJammerErp, Mode=OneTime}"
                                   Minimum="-100" Maximum="200" SmallChange="1"
                                   BorderBrush="{x:Bind ViewModel.JammerErpError, Mode=OneWay,
                                       Converter={StaticResource FieldErrorToBrush}}"
                                   ValueChanged="JammerErpBox_ValueChanged" />
                        <NumberBox x:Name="JammerDistBox"   Header="Jammer→Rx distance (km)"
                                   AutomationProperties.Name="Jammer to Rx distance (km)"
                                   ToolTipService.ToolTip="Ground range from the jammer to the receiver"
                                   Value="{x:Bind ViewModel.DefaultJammerToRxDist, Mode=OneTime}"
                                   Minimum="0.01" Maximum="10000" SmallChange="1"
                                   BorderBrush="{x:Bind ViewModel.JammerDistError, Mode=OneWay,
                                       Converter={StaticResource FieldErrorToBrush}}"
                                   ValueChanged="JammerDistBox_ValueChanged" />
                        <NumberBox x:Name="JammerHeightBox" Header="Jammer height (m)"
                                   AutomationProperties.Name="Jammer height (m)"
                                   ToolTipService.ToolTip="Jammer antenna height above ground — determines the propagation model (LOS or 2-ray) and Fresnel zone crossover distance"
                                   Value="{x:Bind ViewModel.DefaultJammerHeight, Mode=OneTime}"
                                   Minimum="0.1" Maximum="100000" SmallChange="1"
                                   BorderBrush="{x:Bind ViewModel.JammerHeightError, Mode=OneWay,
                                       Converter={StaticResource FieldErrorToBrush}}"
                                   ValueChanged="JammerHeightBox_ValueChanged" />
                        <NumberBox x:Name="HopRangeBox"     Header="Hop range (MHz)"
                                   AutomationProperties.Name="Hop range (MHz)"
                                   ToolTipService.ToolTip="Total frequency-hopping bandwidth of the target signal — set to 0 for a non-hopping (single-channel) signal; partial-band results will show N/A"
                                   Value="{x:Bind ViewModel.DefaultHopRange, Mode=OneTime}"
                                   Minimum="0" Maximum="10000" SmallChange="1"
                                   BorderBrush="{x:Bind ViewModel.HopRangeError, Mode=OneWay,
                                       Converter={StaticResource FieldErrorToBrush}}"
                                   ValueChanged="HopRangeBox_ValueChanged" />
                        <NumberBox x:Name="JsThresholdBox" Header="J/S threshold (dB)"
                                   AutomationProperties.Name="J/S threshold (dB)"
                                   ToolTipService.ToolTip="J/S level at which jamming is considered effective — used to calculate burnthrough range"
                                   Value="{x:Bind ViewModel.DefaultJsThreshold, Mode=OneTime}"
                                   Minimum="-30" Maximum="30" SmallChange="0.5"
                                   BorderBrush="{x:Bind ViewModel.JsThresholdError, Mode=OneWay,
                                       Converter={StaticResource FieldErrorToBrush}}"
                                   ValueChanged="JsThresholdBox_ValueChanged" />
                    </StackPanel>
                </Border>

                <TextBlock Text="SHARED" Style="{StaticResource SectionHeaderStyle}" />
                <Border Style="{StaticResource InputCardStyle}">
                    <StackPanel Spacing="12">
                        <NumberBox x:Name="FrequencyBox"    Header="Frequency (MHz)"
                                   AutomationProperties.Name="Frequency (MHz)"
                                   ToolTipService.ToolTip="Carrier frequency — used for both signal and jammer path-loss calculations"
                                   Value="{x:Bind ViewModel.DefaultFrequency, Mode=OneTime}"
                                   Minimum="0.1" Maximum="100000" SmallChange="10"
                                   BorderBrush="{x:Bind ViewModel.FrequencyError, Mode=OneWay,
                                       Converter={StaticResource FieldErrorToBrush}}"
                                   ValueChanged="FrequencyBox_ValueChanged" />
                        <NumberBox x:Name="RxHeightBox"     Header="Rx height (m)"
                                   AutomationProperties.Name="Rx height (m)"
                                   ToolTipService.ToolTip="Receiver antenna height above ground — determines the propagation model (LOS or 2-ray) and Fresnel zone crossover distance"
                                   Value="{x:Bind ViewModel.DefaultRxHeight, Mode=OneTime}"
                                   Minimum="0.1" Maximum="100000" SmallChange="1"
                                   BorderBrush="{x:Bind ViewModel.RxHeightError, Mode=OneWay,
                                       Converter={StaticResource FieldErrorToBrush}}"
                                   ValueChanged="RxHeightBox_ValueChanged" />
                        <NumberBox x:Name="RxGainSignalBox" Header="Rx gain → signal (dB)"
                                   AutomationProperties.Name="Rx gain to signal (dB)"
                                   ToolTipService.ToolTip="Receive antenna gain toward the signal transmitter — use the main lobe gain if the receiver antenna is pointed at the signal"
                                   Value="{x:Bind ViewModel.DefaultRxGainSignal, Mode=OneTime}"
                                   Minimum="-30" Maximum="60" SmallChange="0.5"
                                   BorderBrush="{x:Bind ViewModel.RxGainSignalError, Mode=OneWay,
                                       Converter={StaticResource FieldErrorToBrush}}"
                                   ValueChanged="RxGainSignalBox_ValueChanged" />
                        <NumberBox x:Name="RxGainJammerBox" Header="Rx gain → jammer (dB)"
                                   AutomationProperties.Name="Rx gain to jammer (dB)"
                                   ToolTipService.ToolTip="Receive antenna gain toward the jammer — a directional antenna with low sidelobes can reject an off-axis jammer by 20–30 dB; sidelobes are typically −13 to −20 dBc"
                                   Value="{x:Bind ViewModel.DefaultRxGainJammer, Mode=OneTime}"
                                   Minimum="-30" Maximum="60" SmallChange="0.5"
                                   BorderBrush="{x:Bind ViewModel.RxGainJammerError, Mode=OneWay,
                                       Converter={StaticResource FieldErrorToBrush}}"
                                   ValueChanged="RxGainJammerBox_ValueChanged" />
                    </StackPanel>
                </Border>
            </StackPanel>

            <TextBlock Text="J/S ANALYSIS" Style="{StaticResource SectionHeaderStyle}" />
            <Border Style="{StaticResource ResultCardStyle}">
                <StackPanel Spacing="0">
                    <local:ResultRow Label="J/S ratio"    Value="{x:Bind ViewModel.JsRatio,     Mode=OneWay}"
                                     ToolTipService.ToolTip="Jammer-to-signal power ratio at the receiver input (dB)" />
                    <local:ResultRow Label="Signal at Rx" Value="{x:Bind ViewModel.SignalAtRx,  Mode=OneWay}"
                                     ToolTipService.ToolTip="Received signal power at the receiver input" />
                    <local:ResultRow Label="Jammer at Rx" Value="{x:Bind ViewModel.JammerAtRx,  Mode=OneWay}"
                                     ToolTipService.ToolTip="Received jammer power at the receiver input" />
                    <local:ResultRow Label="Burnthrough"  Value="{x:Bind ViewModel.Burnthrough, Mode=OneWay}"
                                     ToolTipService.ToolTip="Signal range at which J/S falls to the threshold — the jammer becomes ineffective beyond this distance" />
                </StackPanel>
            </Border>

            <TextBlock Text="PARTIAL-BAND" Style="{StaticResource SectionHeaderStyle}" />
            <Border Style="{StaticResource ResultCardStyle}">
                <StackPanel Spacing="0">
                    <local:ResultRow Label="Optimum BW" Value="{x:Bind ViewModel.OptimumBw, Mode=OneWay}"
                                     ToolTipService.ToolTip="Narrowing the jamming bandwidth increases instantaneous J/S at the cost of hit probability; this is the optimum trade-off" />
                    <local:ResultRow Label="Duty cycle" Value="{x:Bind ViewModel.DutyCycle, Mode=OneWay}"
                                     ToolTipService.ToolTip="Fraction of the hopping band covered by the optimum jamming bandwidth" />
                </StackPanel>
            </Border>

            <Expander Header="Geometry"
                      AutomationProperties.Name="Geometry diagrams"
                      ToolTipService.ToolTip="Show or hide the geometry diagrams for this page"
                      HorizontalAlignment="Stretch"
                      HorizontalContentAlignment="Stretch">
                <StackPanel Spacing="8">
                    <Image Source="ms-appx:///Assets/Diagrams/jamming-self-protection.png"
                           MaxWidth="640" Stretch="Uniform" HorizontalAlignment="Center"
                           AutomationProperties.Name="jamming self protection" />
                    <Image Source="ms-appx:///Assets/Diagrams/jamming-stand-off.png"
                           MaxWidth="640" Stretch="Uniform" HorizontalAlignment="Center"
                           AutomationProperties.Name="jamming stand off" />
                </StackPanel>
            </Expander>

        </StackPanel>
    </ScrollViewer>
</Page>
