<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 150 30" font-family="Georgia, 'Times New Roman', serif" font-size="15">
  <!-- Link margin, standard (and only) form: M = P_rx - S.
       Mid-gray (#808080) like the diagram masters so one asset serves
       light and dark themes. Alt text lives in the data layer.
       NOTE: all formula masters use font-size 15 — see fspl-std.svg. -->
  <g fill="#808080">
    <text x="8" y="20"><tspan font-style="italic">M</tspan> = <tspan font-style="italic">P</tspan><tspan font-size="10" dy="4">rx</tspan><tspan dy="-4">&#8203;</tspan> &#8722; <tspan font-style="italic">S</tspan>  <tspan font-size="10">dB</tspan></text>
  </g>
</svg>
